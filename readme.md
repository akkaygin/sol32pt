This repo was made public to prove that I "know" C, **do not expect anything else**, actually I can use Python and write regex too, anyway.
The assembler cannot assemble to anything (properly) because (my own) ISA is not defined properly, but it did produce a valid program at least once. The main thing is the parser, it is a simple recursive descent parser that builds an AST that I could use to produce meaningful IR or assembly, but again I do not have a good language specification (of my own programming language) which I can build on so it is kind of useless too.
# ~~SOL32 Programming Toolchain~~
~~This repo contains the Launchpad preprocessor, Rocket compiler for the Apogee programming language and an assembler for SOL32 processors.~~
